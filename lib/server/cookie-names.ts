/**
 * Cookie name constants — safe to import from both the Edge middleware and
 * Node server code (no `server-only`, no next/headers dependency).
 */
export const ACCESS_COOKIE = "eailt_access";
export const REFRESH_COOKIE = "eailt_refresh";
